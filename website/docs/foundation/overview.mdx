---
sidebar_position: 0
sidebar_label: Overview
pagination_label: Foundation
---

import { FOUNDATION_EXISTING_ITEMS } from "../../src/data/data";
import { ComponentIndex } from "../../src/components/ComponentIndex";

# Foundation

`eridu-tech` is a modular, adapter-first TypeScript library for building
production-ready backend applications. Every component ships with in-memory
adapters for fast, deterministic testing — and pluggable adapters to swap in
real infrastructure without touching your business logic.

<ComponentIndex
    sections={[
        {
            label: "Foundation",
            intro: "The building blocks that power the rest of the ecosystem.",
            items: FOUNDATION_EXISTING_ITEMS,
        },
    ]}
/>
